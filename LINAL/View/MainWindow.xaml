<Window x:Class="LINAL.view.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LINAL"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.RowSpan="2" Background="black">
            
        </Grid>
        <Canvas Grid.Row="0" Grid.Column="1" Name="drawingPaperFront" Background="Red" />
        <Canvas Grid.Row="0" Grid.Column="2" Name="drawingPaperTop" Background="Yellow" />
        <Canvas Grid.Row="1" Grid.Column="1" Name="drawingPaperLeft" Background="Green" />
        <Canvas Grid.Row="1" Grid.Column="2" Name="drawingPaperRight" Background="Blue" />
    </Grid>
</Window>
